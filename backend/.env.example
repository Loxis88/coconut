# Database
DATABASE_URL=postgres://user:password@localhost:5432/coconut?sslmode=disable

# Auth
JWT_SECRET=change-me-local-secret

# SMTP (optional — leave empty to disable email sending)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=

# Честный Знак fallback API
CHESTNY_ZNAK_API_KEY=

# OpenAI (ingredient extraction + category embedding)
OPENAI_API_KEY=

# Server
PORT=8080
